PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/threat_detection
JWT_SECRET=change_this_to_a_long_random_string
PYTHON_BIN=python3
# Optional: path to a trained ML model (.pkl). Leave unset to use rule-based detection only.
ML_MODEL_PATH=
# Comma-separated list of allowed frontend origins for CORS
CLIENT_ORIGIN=http://localhost:5173
